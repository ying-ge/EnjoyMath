# 💻 新电脑安装指南

## 📋 系统要求

### 必需环境
- **Python 3.8+** （后端服务）
- **Node.js 16+** （前端服务）
- **npm 或 yarn** （Node.js 包管理器）

### 推荐配置
- 操作系统：macOS / Windows / Linux
- 内存：至少 2GB
- 磁盘空间：至少 500MB
- 网络：需要网络连接（下载依赖包）

---

## 🚀 快速安装步骤

### 步骤 1：检查环境

#### 检查 Python
```bash
python3 --version
# 或
python --version
```
**要求**：Python 3.8 或更高版本

如果未安装 Python：
- **macOS**: `brew install python3` 或从 [python.org](https://www.python.org/downloads/) 下载
- **Windows**: 从 [python.org](https://www.python.org/downloads/) 下载安装包
- **Linux**: `sudo apt install python3 python3-pip` (Ubuntu/Debian)

#### 检查 Node.js
```bash
node --version
npm --version
```
**要求**：Node.js 16 或更高版本

如果未安装 Node.js：
- 从 [nodejs.org](https://nodejs.org/) 下载安装包
- 或使用包管理器：
  - macOS: `brew install node`
  - Linux: `sudo apt install nodejs npm`

---

### 步骤 2：获取项目文件

#### 方式一：从 GitHub 克隆（如果已上传）
```bash
git clone <你的仓库地址>
cd EnjoyMath
```

#### 方式二：直接复制项目文件夹
将整个 `EnjoyMath` 文件夹复制到新电脑

---

### 步骤 3：安装后端依赖

```bash
# 进入项目目录
cd EnjoyMath

# 安装 Python 依赖
pip3 install -r requirements.txt

# 或者使用虚拟环境（推荐）
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

**依赖列表**：
- Flask==3.0.0
- Flask-CORS==4.0.0
- numpy==1.24.3
- scipy==1.11.4
- sqlalchemy==2.0.23
- flask-sqlalchemy==3.1.1

---

### 步骤 4：安装前端依赖

```bash
# 进入前端目录
cd frontend

# 安装 Node.js 依赖
npm install

# 等待安装完成（可能需要几分钟）
```

**依赖列表**：
- React 18
- TypeScript
- Vite
- Axios
- KaTeX (数学公式渲染)
- React Router

---

### 步骤 5：初始化数据库

```bash
# 在项目根目录
cd backend

# 初始化数据库（会自动创建数据库并导入题目）
python3 app.py --init-db
```

这将会：
- 创建 SQLite 数据库文件
- 导入所有 85 道题目
- 初始化数据库表结构

---

### 步骤 6：启动服务

#### 终端 1：启动后端服务
```bash
cd EnjoyMath/backend
python3 app.py
```

看到以下信息表示启动成功：
```
==================================================
数学水平测试系统 - 后端服务
==================================================
服务地址: http://0.0.0.0:5001
API文档: http://0.0.0.0:5001/api/health
==================================================
 * Running on http://0.0.0.0:5001
```

#### 终端 2：启动前端服务
```bash
cd EnjoyMath/frontend
npm run dev
```

看到以下信息表示启动成功：
```
  VITE v5.0.8  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

---

### 步骤 7：访问系统

在浏览器中打开：
```
http://localhost:3000
```

---

## 📦 完整安装清单

### Python 环境（后端）
- [ ] Python 3.8+
- [ ] pip（Python 包管理器）
- [ ] 安装后端依赖：`pip install -r requirements.txt`

### Node.js 环境（前端）
- [ ] Node.js 16+
- [ ] npm（Node.js 包管理器）
- [ ] 安装前端依赖：`cd frontend && npm install`

### 项目文件
- [ ] 项目代码文件夹
- [ ] 数据库初始化：`python3 backend/app.py --init-db`

---

## 🔧 常见问题解决

### 问题 1：Python 命令不存在
**macOS/Linux**: 使用 `python3` 而不是 `python`
**Windows**: 确保在安装 Python 时勾选"Add Python to PATH"

### 问题 2：pip 命令不存在
```bash
# macOS/Linux
python3 -m ensurepip --upgrade

# Windows
python -m ensurepip --upgrade
```

### 问题 3：npm 安装慢或失败
```bash
# 使用国内镜像（可选）
npm config set registry https://registry.npmmirror.com

# 或者使用 yarn
npm install -g yarn
yarn install
```

### 问题 4：端口被占用
如果 5001 或 3000 端口被占用：

**修改后端端口**（`backend/app.py`）：
```python
port = int(os.environ.get('PORT', 5002))  # 改为 5002
```

**修改前端端口**（`frontend/vite.config.ts`）：
```typescript
server: {
  port: 3001,  // 改为 3001
}
```

### 问题 5：数据库初始化失败
```bash
# 删除旧数据库文件（如果存在）
rm backend/math_test.db

# 重新初始化
python3 backend/app.py --init-db
```

### 问题 6：依赖安装失败

**Python 依赖**：
```bash
# 升级 pip
pip3 install --upgrade pip

# 逐个安装
pip3 install Flask==3.0.0
pip3 install Flask-CORS==4.0.0
# ... 其他依赖
```

**Node.js 依赖**：
```bash
# 清除缓存
npm cache clean --force

# 删除 node_modules 重新安装
rm -rf node_modules package-lock.json
npm install
```

---

## 📝 安装脚本（自动化）

### macOS/Linux 一键安装脚本

创建 `install.sh` 文件：
```bash
#!/bin/bash
echo "开始安装数学水平测试系统..."

# 检查 Python
if ! command -v python3 &> /dev/null; then
    echo "错误: 未找到 Python3，请先安装 Python 3.8+"
    exit 1
fi

# 检查 Node.js
if ! command -v node &> /dev/null; then
    echo "错误: 未找到 Node.js，请先安装 Node.js 16+"
    exit 1
fi

# 安装后端依赖
echo "安装后端依赖..."
pip3 install -r requirements.txt

# 安装前端依赖
echo "安装前端依赖..."
cd frontend
npm install
cd ..

# 初始化数据库
echo "初始化数据库..."
cd backend
python3 app.py --init-db
cd ..

echo "安装完成！"
echo "启动后端: cd backend && python3 app.py"
echo "启动前端: cd frontend && npm run dev"
```

运行：
```bash
chmod +x install.sh
./install.sh
```

### Windows 安装脚本

创建 `install.bat` 文件：
```batch
@echo off
echo 开始安装数学水平测试系统...

:: 检查 Python
python --version >nul 2>&1
if errorlevel 1 (
    echo 错误: 未找到 Python，请先安装 Python 3.8+
    pause
    exit /b 1
)

:: 检查 Node.js
node --version >nul 2>&1
if errorlevel 1 (
    echo 错误: 未找到 Node.js，请先安装 Node.js 16+
    pause
    exit /b 1
)

:: 安装后端依赖
echo 安装后端依赖...
pip install -r requirements.txt

:: 安装前端依赖
echo 安装前端依赖...
cd frontend
call npm install
cd ..

:: 初始化数据库
echo 初始化数据库...
cd backend
python app.py --init-db
cd ..

echo 安装完成！
echo 启动后端: cd backend && python app.py
echo 启动前端: cd frontend && npm run dev
pause
```

---

## 🎯 验证安装

安装完成后，运行以下命令验证：

```bash
# 检查 Python 依赖
python3 -c "import flask, numpy, scipy; print('✅ Python 依赖安装成功')"

# 检查 Node.js 依赖
cd frontend
npm list --depth=0 | grep react && echo "✅ Node.js 依赖安装成功"
cd ..

# 检查数据库
cd backend
python3 -c "from app import app; from models import Question; app.app_context().push(); print(f'✅ 数据库正常，有 {Question.query.count()} 道题目')"
```

---

## 📚 相关文档

- **快速开始**: 查看 `README.md`
- **使用指南**: 查看 `使用指南.md`
- **题库说明**: 查看 `README_QUESTIONS.md`

---

## ✅ 安装检查清单

在开始使用前，确保：

- [ ] Python 3.8+ 已安装
- [ ] Node.js 16+ 已安装
- [ ] 后端依赖已安装（pip install -r requirements.txt）
- [ ] 前端依赖已安装（npm install）
- [ ] 数据库已初始化（85 道题目）
- [ ] 后端服务可以启动（端口 5001）
- [ ] 前端服务可以启动（端口 3000）
- [ ] 浏览器可以访问 http://localhost:3000

---

安装完成后，就可以开始使用系统了！🎉

